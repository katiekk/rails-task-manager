<%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_area :details %>
  <%= f.check_box :completed %>
  <%= f.label :completed %>
  <%= f.submit 'Update Task' %>
<% end %>
<hr>
<%= link_to "Back to task", task_path(@task) %>
